[package]
name = "ofan"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "ofan"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
thiserror = "2.0"
inkwell = { version = "0.9", features = ["llvm18-1"], optional = true }

[features]
codegen = ["inkwell"]
